# Weather Now — Starter

Your first app that talks to the internet — search a city (two chained API calls), loading/error/data, never a crash (Module 11). The http package is pre-added.

## Your mission
Full brief + rubric: `challenges/module-11/weather-now.md` in this repo.

## Run it
```
flutter pub get
flutter run
```
You should see an empty screen with a TODO in `lib/main.dart`. Everything visible in the finished app, **you** type.
