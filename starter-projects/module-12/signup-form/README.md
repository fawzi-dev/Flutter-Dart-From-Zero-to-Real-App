# Sign Up — Starter

A real registration form: a `Form` with a `GlobalKey`, `TextFormField`s with validators, a dropdown, a "must agree" checkbox, focus flow between fields, and a submit that validates everything at once (Module 12).

## Your mission
Full brief + rubric: `challenges/module-12/signup-form.md` in this repo.

## Run it
```
flutter pub get
flutter run
```
You should see a near-empty screen with a TODO in `lib/main.dart`. Everything visible in the finished app, **you** type.
