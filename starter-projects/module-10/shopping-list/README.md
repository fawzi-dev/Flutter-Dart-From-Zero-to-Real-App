# Shopping List — Starter

List, add screen, check off, delete — the canonical real app, built across multiple files with a shared design system (Module 10).

## Your mission
Full brief + rubric: `challenges/module-10/shopping-list.md` in this repo.

## Run it
```
flutter pub get
flutter run
```
You should see an empty screen with a TODO in `lib/main.dart`. Everything visible in the finished app, **you** type.
