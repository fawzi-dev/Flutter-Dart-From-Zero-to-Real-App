# Dicee — Starter

Two dice + a ROLL button. Layout this module (8), it comes alive next module (9).

## Your mission
Full brief + rubric: `challenges/module-08/dicee-ui.md` in this repo.

## Run it
```
flutter pub get
flutter run
```
You should see an empty screen with a TODO in `lib/main.dart`. Everything visible in the finished app, **you** type.
