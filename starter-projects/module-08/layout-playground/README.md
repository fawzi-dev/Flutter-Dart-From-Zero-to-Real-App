# Layout Playground — Starter

The playground for Module 8. `ColorCard`, the colours and the spacing scale
are already written, so every lesson is only about layout.

## Run it
```
flutter pub get
flutter run
```
You should see an empty screen. Each lesson, you replace the `SizedBox()`
inside the `Padding` in `lib/main.dart` with the layout being taught.
