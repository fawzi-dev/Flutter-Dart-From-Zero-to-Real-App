# Ticket Booking — Starter

The screen you build across Module 8. The colours, the spacing scale and the
poster image (`assets/event.jpg`) are already in place, so every lesson is
only about layout.

## Run it
```
flutter pub get
flutter run
```
You should see an empty screen. Everything on the ticket, **you** type in
`lib/main.dart`.
